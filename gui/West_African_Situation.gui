widget = {
    name = "gate_popup_container"
    allow_outside = yes
    size = { 100% 100% }
    layer = popups
    visible = "[GetVariableSystem.Exists('show_my_window')]"

    missing_dependency_popup = {}
}
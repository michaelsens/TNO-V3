law_draconian_brutality = {
	group = lawgroup_penal_system
	icon = "gfx/interface/icons/law_icons/prison_evil.dds"
	
	progressiveness = -50
	
	modifier = {
		country_authority_add = 100
		country_suppression_ig_attraction_mult = 0.5
		state_mortality_mult = 0.07
		state_mortality_turmoil_mult = 0.1
		country_weekly_innovation_add = -10
		state_turmoil_effects_mult = -0.40
	}

	institution = institution_prison
	institution_modifier = {
		state_radicals_from_sol_change_mult = -0.05
		state_radicals_from_discrimination_mult = -0.05
		state_mortality_turmoil_mult = 0.05
		political_movement_radicalism_mult = -0.05
	}
	
	possible_political_movements = {
		law_censorship
		law_overt_oppresion
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_protected_speech
						} 
					}
					is_pop_type = academics
				}
				value = 0.5
			}
		}
		# People don't like dieing in prison
		add = {
			desc = "ALL_POPS"
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_draconian_brutality
						}
					}
				}
				value = 0.01
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_overt_oppresion = {
	group = lawgroup_penal_system
	icon = "gfx/interface/icons/law_icons/prison_overt.dds"
	
	progressiveness = -40
	
	modifier = {
		country_authority_add = 60
		country_suppression_ig_attraction_mult = 0.3
		state_mortality_mult = 0.01
		state_mortality_turmoil_mult = 0.05
		country_weekly_innovation_add = -5
		state_turmoil_effects_mult = -0.30
	}
	
	possible_political_movements = {
		law_draconian_brutality
		law_protected_speech
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_protected_speech
						} 
					}
					is_pop_type = academics
				}
				value = 0.5
			}
		}
		# People don't like dieing in prison
		add = {
			desc = "ALL_POPS"
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_draconian_brutality
						}
					}
				}
				value = 0.01
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_millitary_justice = {
	group = lawgroup_penal_system
	icon = "gfx/interface/icons/law_icons/prison_milll.dds"
	
	progressiveness = -30
	
	modifier = {
		country_authority_add = 50
		country_suppression_ig_attraction_mult = 0.1
		state_mortality_mult = 0.01
		state_mortality_turmoil_mult = 0.1
		country_weekly_innovation_add = -2
		interest_group_ig_armed_forces_pol_str_mult = 0.10
		state_turmoil_effects_mult = -0.25
	}
	
	possible_political_movements = {
		law_draconian_brutality
		law_protected_speech
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_protected_speech
						} 
					}
					is_pop_type = academics
				}
				value = 0.5
			}
		}
		# People don't like dieing in prison
		add = {
			desc = "ALL_POPS"
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_draconian_brutality
						}
					}
				}
				value = 0.01
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_privet_prisons= {
	group = lawgroup_penal_system
	icon = "gfx/interface/icons/law_icons/prison_cap.dds"
	
	progressiveness = -20
	
	modifier = {
		interest_group_ig_industrialists_pol_str_mult = 0.1
		state_capitalists_investment_pool_contribution_add = 0.1
		country_authority_add = 20
			state_turmoil_effects_mult = -0.15
	}
	
	possible_political_movements = {
		law_draconian_brutality
		law_protected_speech
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_protected_speech
						} 
					}
					is_pop_type = academics
				}
				value = 0.5
			}
		}
		# People don't like dieing in prison
		add = {
			desc = "ALL_POPS"
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_draconian_brutality
						}
					}
				}
				value = 0.01
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_mass_incarceration = {
	group = lawgroup_penal_system
	icon = "gfx/interface/icons/law_icons/prison_mass.dds"
	
	progressiveness = -10
	
	modifier = {
		state_radicals_from_discrimination_mult = 0.25
		state_education_access_add	= -0.05
		country_authority_add = 10
		state_turmoil_effects_mult = -0.10
	}
	
	possible_political_movements = {
		law_draconian_brutality
		law_protected_speech
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_protected_speech
						} 
					}
					is_pop_type = academics
				}
				value = 0.5
			}
		}
		# People don't like dieing in prison
		add = {
			desc = "ALL_POPS"
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_draconian_brutality
						}
					}
				}
				value = 0.01
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_incarceration = {
	group = lawgroup_penal_system
	icon = "gfx/interface/icons/law_icons/prison_norm.dds"
	
	progressiveness = 10
	
	modifier = {
			state_turmoil_effects_mult = -0.05
			state_loyalists_from_sol_change_mult = 0.1
	}
	
	possible_political_movements = {
		law_draconian_brutality
		law_protected_speech
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_protected_speech
						} 
					}
					is_pop_type = academics
				}
				value = 0.5
			}
		}
		# People don't like dieing in prison
		add = {
			desc = "ALL_POPS"
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_draconian_brutality
						}
					}
				}
				value = 0.01
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_rehabilitation = {
	group = lawgroup_penal_system
	icon = "gfx/interface/icons/law_icons/prison_reha.dds"
	
	progressiveness = 10
	
	modifier = {
			state_turmoil_effects_mult = -0.05
			state_loyalists_from_sol_change_mult = 0.1
	}
	
	possible_political_movements = {
		law_draconian_brutality
		law_protected_speech
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_protected_speech
						} 
					}
					is_pop_type = academics
				}
				value = 0.5
			}
		}
		# People don't like dieing in prison
		add = {
			desc = "ALL_POPS"
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_draconian_brutality
						}
					}
				}
				value = 0.01
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_no_prison = {
	group = lawgroup_penal_system
	icon = "gfx/interface/icons/law_icons/prison_none.dds"
	
	progressiveness = 10
	
	modifier = {
	}
	
	possible_political_movements = {
		law_draconian_brutality
		law_protected_speech
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_protected_speech
						} 
					}
					is_pop_type = academics
				}
				value = 0.5
			}
		}
		# People don't like dieing in prison
		add = {
			desc = "ALL_POPS"
			if = {
				limit = {
					owner = { 
						NOT = { 
							has_law = law_type:law_draconian_brutality
						}
					}
				}
				value = 0.01
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

